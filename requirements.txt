# Requirements for Evidence-Aware KAG-Solver (M2 Offline Reproducible Package)
# Python runtime: >=3.10, tested and verified on Python 3.11.9 (64-bit Windows)

# Core test execution dependencies:
networkx==3.6.1
tenacity>=8.0.0
pydantic>=2.0.0
ruamel.yaml>=0.17.0
pytest>=7.0.0

# Bundled OpenSPG KAG library
./vendor/KAG
